# HedgeDaChip

This repository contains the `apu-prototype` FPGA-first Agent Processing Unit (APU) MVP scaffold.

## Quick start

```bash
cd apu-prototype
python3 -m pip install -r tb/requirements.txt
./sim/cocotb/run.sh
```
